# 实验记录

NEU-DET（train 1440 / val 360，640×640）。所有 mAP 为验证集 mAP@0.5。

> **2026-08-21 根因：CIoU 在 autocast 下的 fp16 溢出。** 分配器收到的是像素坐标
> 的 fp16 预测框，CIoU 要算外接框对角线平方；边长超过约 **181 px** 时 `c^2` 就超过
> fp16 上限 65504，惩罚项变成 inf/nan，对齐度量过不了 `> 0` 判定，**该目标在所有
> 层级上都拿不到正样本**。参考实现用 `.type(gt_bboxes.dtype)` 强制 fp32，我直接传了
> fp16。这一处解释了从第一个实验起就存在的"AP 随框尺寸单调下降"，以及 stride 32
> 从第 1 轮起零正样本。修复后同配置 6 轮从 0.1214 涨到 0.3911。
>
> 溢出阈值与逐类表现完全对应：
>
> | 类别 | 等效边长 | 溢出 | 修复前 AP50 |
> | --- | --- | --- | --- |
> | inclusion | 126 px | 否 | 0.78 |
> | scratches | 174 px | 否 | 0.59 |
> | patches | 196 px | 临界 | 0.84 |
> | rolled-in_scale | 216 px | **是** | 0.22 |
> | crazing | 289 px | **是** | 0.07 |
> | pitted_surface | 489 px | **是** | 0.006 |
>
> **2026-08-21：全部 mAP 数值已作废并重测。** `match_predictions` 把真值的命中
> 记给了 IoU 最大的检测框而非置信度最高的那个，PR 曲线被打乱，AP 系统性腰斩约
> 0.06（对大目标类别更重）。修复见下文"已修复的实现缺陷"。带 † 的行仍是旧数值，
> 需用 `runs/<name>/best.pt` 重测。

## 结果总表

| # | 名称 | 主干 | 关键配置 | mAP50 | 有效性 |
| --- | --- | --- | --- | --- | --- |
| — | `ultra_ref` | YOLOv8n **COCO 预训练** | ultralytics 官方实现 | **0.7124** | ✅ 参考上界 |
| — | `ultra_scratch` | YOLOv8n **从零** | ultralytics 官方实现 | *待测* | ✅ 对等参照 |
| 1 | `tiny_s1234_panet_seed0` | SAM2-T 冻结 | mlp adapter×4，无归一化 | 0.3909 | ⚠️ 管线有 bug |
| 2 | `tiny_norm_e300` | SAM2-T 冻结 | +ImageNet 归一化，200ep | 0.4151 | ⚠️ 同上 |
| 3 | `conv_adapter` | SAM2-T 冻结 | conv adapter，ratio 2 | 0.4151 | ⚠️ 同上 |
| 4 | `fix_sizeaware` | SAM2-T 冻结 | +size-aware 分配 | ~0.31 | ❌ 该策略有害 |
| 5 | `fix_regmax24` | SAM2-T 冻结 | reg_max 24 + size-aware | 0.3140 | ❌ 同上 |
| 6 | `e_mosaic` | SAM2-T 冻结 | +mosaic +多尺度，150ep | 0.3311 | ⚠️ 管线有 bug |
| 7 | `f_cnn_baseline` | **CSPDarknet 从零** | 同 6 的配方 | 0.3112 | ⚠️ 同上 |
| 8 | `g_unfrozen` | **SAM2-T 全量微调** | 同 6 的配方，lr 1e-4 | 0.3805 | ⚠️ 同上 |
| 9 | `i_cnn_fixed` | CSPDarknet 从零 | +分配器修复 | 0.3678 † | ✅ |
| 10 | `j_cnn_noobj` | CSPDarknet 从零 | 9 + 删除 obj 分支 | 0.2948 † | ✅ |
| 11 | `k_biasfix` | CSPDarknet 从零 | 9 + 层级感知 bias 初始化 | **0.4196** | ✅ |
| 12 | `l_ultra_arch` | **YOLOv8n 网络** | 我的循环，60ep，mosaic | 0.2684 † | ✅ 二分对照 |
| 13 | `m_ultra_nomosaic` | YOLOv8n 网络 | 我的循环，60ep，无 mosaic | 0.2124 † | ✅ 二分对照 |
| 14 | `n_hflip_only` | YOLOv8n 网络 | 13 + 只留水平翻转 | 0.2195 † | ✅ |
| 15 | `o_affine` | YOLOv8n 网络 | 14 + 逐图仿射增强 | **0.2930** | ✅ |
| 16 | `p_fp32_cnn` | CSPDarknet 从零 | **分配器 fp32**（根因修复） | **0.5652** | ✅ |
| 17 | `q_visfix` | CSPDarknet 从零 | 16 + `min_box_visibility` 0.3→0.1 | **0.5874** | ✅ |
| 18 | `r_mosaic_scale` | CSPDarknet 从零 | 17 + mosaic 缩放系数均匀采样 | 0.5832 | ✅ 机制生效，mAP 持平 |
| 19 | `s_noobj` | CSPDarknet 从零 | 18 + **删除 obj 分支** | **0.5965** | ✅ |
| 20 | `t_noflip` | CSPDarknet 从零 | 19 + 关闭 vflip/rot90 | 0.5608 | ✅ 反而更差 |
| 21 | `u_nomosaic` | CSPDarknet 从零 | 19 + **完全关闭 mosaic** | **0.6982** | ✅ **当前最佳** |
| — | `ultra_nomosaic` | YOLOv8n 网络 | **他们的循环**，60ep，无 mosaic | **0.6630** | ✅ 对等参照 |

## 已修复的实现缺陷

| 缺陷 | 影响 | 状态 |
| --- | --- | --- |
| 缺 ImageNet 输入归一化（SAM2 要求） | 冻结主干无法自适应分布偏移 | 已修（#2） |
| obj 分数被乘进 TAL 对齐度量 | 层级坍塌的反馈回路 | 已修（#9，+0.057） |
| obj 损失按 8400 anchor 取均值 | 正样本被稀释 200×，分支几乎不训练 | 已修（#9） |
| cls bias 初始化用固定常数 | 初始 cls 损失 105，负样本质量压垮正样本 | 已修（#11） |
| EMA 只平均 neck+head，不含主干 | 评估用"平滑的头 + 漂移了 11 轮的主干" | 已修 |
| `scale`/`translate` 只在 mosaic 内生效 | `--mosaic 0` 时几何增强完全为零 | 已修（逐图仿射） |
| **`match_predictions` 按 IoU 而非置信度分配命中** | **AP 腰斩 ~0.06，对大目标更重；污染了全部实验数值** | **已修** |
| **分配器在 autocast 下用 fp16 算 CIoU** | **>181px 的框全部拿不到正样本；6 轮 mAP 0.12 → 0.39** | **已修（根因）** |
| 评估 NMS 用 `multi_label=False` | 与参考不一致（实测影响很小） | 已对齐 |

## 已验证正确（与 ultralytics 逐位一致）

`python -m model.crosscheck` 全部最大差 = 0.000e+00：

- `make_anchors` 锚点与 stride
- DFL 解码（`dist2bbox` + softmax·proj）
- `bbox2dist` 目标编码
- **TaskAlignedAssigner**：正样本数、fg_mask、target_scores、target_bbox、层级分布

## 关键诊断结论

1. **不是过拟合**：`f_cnn_baseline` 在**训练集**上 mAP 仅 0.3993（验证 0.3112）——模型连见过 150 遍的图都拟合不了。
2. **分类没问题，定位才是瓶颈**：`cls ok`（定位成功后类别是否正确）全类 0.94–0.99。
3. **失败与框的尺寸严格单调相关**：

   | 类别 | 框面积中位 | AP50（#11） |
   | --- | --- | --- |
   | inclusion | 0.039 | 0.659 |
   | scratches | 0.074 | 0.449 |
   | patches | 0.094 | 0.815 |
   | rolled-in_scale | 0.114 | 0.158 |
   | crazing | 0.204 | 0.064 |
   | pitted_surface | **0.540** | **0.006** |

4. **大框失败与主干无关**：随机初始化 CNN（无 SAM2）同样崩在 pitted_surface 0.006；COCO 预训练 YOLOv8n（无 SAM2）却有 0.797。**排除了"SAM2 偏好细小分割"这一假设。**
5. **头和损失本身能拟合任意尺寸**：`python -m model.fittest` 单图单框训练 300 步，608×608 的框也能到 IoU 0.998。
6. **损失量级对比**（原始值，已还原权重）：

   | | 参考 | 我们（#11） |
   | --- | --- | --- |
   | box `1-IoU` | 0.131（IoU 0.87） | 0.302（IoU 0.70） |
   | cls | 1.525 | 2.627 |
   | dfl | 0.852 | 1.231 |

   全面偏弱而非单点崩溃 → 指向特征质量/预训练，而非某个 bug。

## 主干与 adapter 消融（5 配置 × 3 种子，2026-08-22）

| 配置 | 均值 mAP50 | 标准差 | 可训练 | GFLOPs | FPS |
| --- | --- | --- | --- | --- | --- |
| SAM2-T + 全 adapter | 0.7009 | 0.0078 | 4.25M | 100.6 | 50.4 |
| **CNN 从零** | **0.6996** | 0.0016 | 8.47M | 34.1 | **146.7** |
| SAM2-T 无 adapter | 0.6975 | 0.0074 | 4.14M | 99.6 | 55.6 |
| SAM2-T + adapter@4 | 0.6963 | 0.0028 | 4.22M | 99.8 | 55.3 |
| SAM2-T + adapter@3,4 | 0.6910 | 0.0061 | 4.24M | 100.1 | 54.0 |

```
单因素 ANOVA: F(4,10) = 1.363，临界值 3.48 → 组间差异不显著
合并组内标准差 0.0057；n=3 可检出的最小差异 ≈ 0.0135；实际最大组间差异 0.0100
```

**结论（措辞需精确）**：在本实验精度下未能测出 adapter 配置或 SAM2 骨干的效应，
而非"效应为零"。单种子时观察到的 0.015 极差是噪声。

加上外部参照，六种配置横跨 16 倍算力、两种检测范式、随机初始化与基础模型预训练：

| 配置 | GFLOPs | mAP50 |
| --- | --- | --- |
| YOLOv8n 从零（+mosaic） | 8.1 | 0.7135 |
| SAM2-T 各配置 | ~100 | 0.691–0.701 |
| CNN 从零 | 34.1 | 0.6996 |
| YOLO26n 端到端 | 6.1 | 0.6602 |

**主干不是瓶颈。**

## 弥散缺陷的实例数量歧义（2026-08-22）

同一诊断流程、同一批图，只换权重：

| 类别 | 真值 | 我们 pred n | 参考 pred n | 我们 AP50 | 参考 AP50 |
| --- | --- | --- | --- | --- | --- |
| crazing | 162 | 815 (5.0x) | 826 (5.1x) | 0.391 | 0.471 |
| rolled-in_scale | 132 | 604 (4.6x) | 533 (4.0x) | 0.492 | 0.519 |
| patches | 193 | 349 (1.8x) | 337 (1.7x) | 0.907 | 0.924 |

crazing 上我们定位更准（medIoU 0.719 vs 0.690、IoU>=.5 0.864 vs 0.852）、分类更对
（1.000 vs 0.994）、框尺寸更接近真值（0.238 vs 0.211，真值 0.242），AP 却低 0.08。
**两个独立实现都吐出 5 倍于真值的高置信度框**，说明弥散缺陷的过预测是任务固有的：
边界不模糊，模糊的是"该框几个"。剩余的 0.08 是置信度排序质量问题，与之无关。

## 修复后的逐类进展

| 类别 | 等效边长 | 起点 | fp32 后 | **当前(#19)** | 参考 |
| --- | --- | --- | --- | --- | --- |
| inclusion | 126 px | 0.780 | 0.736 | 0.713 | 0.752 |
| scratches | 174 px | 0.594 | 0.725 | 0.630 | 0.822 |
| patches | 196 px | 0.843 | 0.895 | 0.884 | 0.924 |
| rolled-in_scale | 216 px | 0.225 | 0.440 | 0.477 | 0.519 |
| crazing | 289 px | 0.070 | 0.286 | 0.340 | 0.471 |
| pitted_surface | 489 px | **0.006** | 0.308 | **0.535** | 0.793 |
| **总计** | | **0.3585** | 0.5652 | **0.5965** | 0.7135 |

obj 分支的删除单独带来 pitted_surface +0.15：它以 IoU 为目标训练，推理时以
`cls * obj` 参与排序，对大框系统性给低分，于是"找得到但排不上去"。参考实现
（YOLOv8）没有这个分支，**design.md 里的三分支头设计需要据此修订**。

## mosaic：净负贡献 −0.10（已关闭）

关闭 mosaic 后 mAP50 从 0.5965 升到 **0.6982**，mAP50-95 从 0.2571 升到 **0.3607**
（参考 0.7135 / 0.3617）。stride 32 的正样本从 116 升到 224。逐类看，scratches
(0.886) 和 inclusion (0.785) 已经超过参考实现。

我们的 mosaic 与参考的差异（4 图拼接后裁剪重采样 vs 在画布上做仿射）在这个数据集
上是有害的，且量级远大于所有其他调优项之和。保留在代码中供消融，默认关闭。

### 关闭前的诊断记录

| | e60 | e90 | e120 | 最终 |
| --- | --- | --- | --- | --- |
| 参考（mosaic 全程开启） | 0.6723 | 0.6586 | — | 0.7135 |
| 我们 + mosaic（#19） | 0.3686 | 0.3742 | 0.3651 | 0.5965 |
| **我们无 mosaic（#21）** | — | — | — | **0.6982** |

线索来自"mosaic 期间差 0.30、收尾后差 0.12"这个落差。

| | e60 | e90 | e120 | 最终 |
| --- | --- | --- | --- | --- |
| 参考（mosaic 全程开启） | **0.6723** | 0.6586 | — | 0.7135 |
| 我们（#19） | 0.3686 | 0.3742 | 0.3651 | 0.5965 |

参考实现在 mosaic 开启期间就已经达到 0.67；我们在同期只有 0.37，最终的 0.5965 里有
**+0.23 来自 `--close-mosaic` 后的最后 15 个干净轮次**。这说明我们的 mosaic 产出的
分布与干净验证集之间存在参考实现没有的落差——模型在拟合 mosaic 图而迁移不到原图。
mosaic 期间差 0.30，收尾后差 0.12，量级远大于其他候选项。

## 为什么这个 bug 藏了十几轮

三个特征叠加：

1. **只在 autocast 下出现。** `crosscheck` / `losscheck` / `predcheck` 全部跑在 fp32
   里，每次对拍都显示"逐位一致"——它们验证的是代码逻辑，而 bug 在数值精度上。
2. **只影响大框。** 小目标类别始终正常（inclusion 0.78），把问题伪装成"大目标本来
   就难"，诱导了好几轮关于 reg_max、层级分配、SAM2 分割先验的错误归因。
3. **不产生 nan，不崩溃。** 损失曲线完全正常，只是部分目标静默地拿不到正样本。

教训：当一个规律**跨越所有配置、所有主干都稳定成立**时（这里是"AP 随框尺寸单调
下降"，对随机初始化 CNN 和预训练 SAM2 一样成立），它多半来自两者共用的代码路径，
应当优先怀疑数值问题而非模型能力。诊断工具里的 `pred area` 和层级归因其实已经给出
了信号，但被归因成了"DFL 回归范围不够"。

## 被推翻的假设（避免重复排查）

| 假设 | 推翻依据 |
| --- | --- |
| SAM2 特征存在域差距，纹理类迁移不了 | `cls ok` 0.94–0.99，分类完全正常 |
| DFL 回归范围（reg_max）不够 | `fittest` 证明 608px 框能拟合；reg_max 24/32 无增益 |
| 层级分配需要尺寸约束 | size-aware 分配掉 0.10 mAP（细长目标被赶到粗层级） |
| adapter 容量/空间感知不足 | mlp→conv、ratio 4→2 均无变化 |
| 冻结主干是天花板 | 全量微调（#8）仅 0.3805 |
| 增强不足 | 加 mosaic 后反而从 0.4151 掉到 0.3311 |
| obj 分支导致层级坍塌 | 修复后坍塌仍在（换了个层级）；删除 obj 更差 |
| SAM2 的分割先验导致偏好小目标 | 无 SAM2 的 CNN 同样崩在大框 |
| 垂直翻转/90°旋转破坏了轧制方向线索 | 关掉后 scratches 从 0.630 掉到 0.446（#20）；1440 张图上样本多样性的价值高于方向线索 |

## 待办

- [ ] `ultra_scratch`：从零 YOLOv8n，判定大框失败是实现缺陷还是预训练效应
- [ ] mosaic 标签正确性目视复核（`python -m model.visualize --aug`）
- [ ] 若实现无缺陷 → 转向"基础模型特征替代 COCO 预训练"的命题
- [ ] 若仍有缺陷 → 继续对拍 neck / head / 训练循环

## 复现命令

```bash
python -m model.selftest --imgsz 640          # 结构自检
python -m model.crosscheck                    # 与 ultralytics 逐位对拍
python -m model.fittest                       # 单图单框拟合能力
python -m model.diagnose --weights <ckpt>     # 定位/分类解耦诊断 + 层级归因
python -m model.visualize --aug               # 增强样本目视检查
```
